# --- Web layer ---
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.6
python-dotenv>=1.0

# --- Data layer ---
sqlalchemy>=2.0
# Use psycopg2-binary for Postgres. SQLite works out of the box for dev.
psycopg2-binary>=2.9
pydantic>=2.5
pydantic-settings>=2.1

# --- Ingestion / image ---
pymupdf>=1.23
pillow>=10.0
numpy>=1.24
opencv-python-headless>=4.9

# --- LLM stack (agentic) ---
anthropic>=0.40
langchain>=0.3
langchain-anthropic>=0.2
langchain-google-genai>=2.0
langchain-groq>=0.2
google-generativeai>=0.8
langgraph>=0.2

# --- Grounding (HHEM) + embeddings / plagiarism ---
# HHEM-2.1-Open needs transformers 4.x; 5.x breaks its custom model code.
# sentence-transformers pinned <6 so it does not pull transformers 5.x back in.
transformers>=4.40,<5
huggingface-hub>=0.30,<1
torch>=2.2
sentence-transformers>=2.5,<6
scikit-learn>=1.4

# --- Optional: Qwen-VL via HuggingFace (requires GPU) ---
# Uncomment to enable the qwen_vl adapter. Demo runs without these.
# accelerate>=0.30